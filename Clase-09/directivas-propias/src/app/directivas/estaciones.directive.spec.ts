import { EstacionesDirective } from './estaciones.directive';

describe('EstacionesDirective', () => {
  it('should create an instance', () => {
    const directive = new EstacionesDirective();
    expect(directive).toBeTruthy();
  });
});
