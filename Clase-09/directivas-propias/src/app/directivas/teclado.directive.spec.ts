import { TecladoDirective } from './teclado.directive';

describe('TecladoDirective', () => {
  it('should create an instance', () => {
    const directive = new TecladoDirective();
    expect(directive).toBeTruthy();
  });
});
